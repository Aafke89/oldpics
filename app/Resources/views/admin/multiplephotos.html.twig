{% extends 'base.html.twig' %}
{% use "form_div_layout.html.twig" %}
{% block body %}
    <div class="welcome">
        <div class="text-center">
            <h1>Add your photo's here</h1>
        </div>
        {{ form_start(form) }}
        {{ form_row(form.files) }}
        {{ form_row(form.folder) }}
        <button type="submit" class="btn btn-primary" formnovalidate>Add</button>
        {{ form_end(form) }}
    </div>
{% endblock %}