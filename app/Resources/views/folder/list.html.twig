{% extends 'base.html.twig' %}
{% block body %}
   <div class="folder-foto">
       <div class="folder-tekstje">
           <p>Hoi, hier zijn alle mapjes met foto's die we hebben toegevoegd. Klik op een mapje om alle foto's in dat mapje te kunnen zien!</p>
       </div>
       <div class="container">
           <div class="row">
               {% for folder in folders  %}
                   <div class="col-xs-3">
                       <div class="folder-preview">
                           <b>{{ folder.title }}</b><br>
                           <div class="folder-preview-image">
                               <a href="{{ path('folder_show', {'folderId': folder.id }) }}"><img src="/uploads/photos/{{ folder.photos|first.file }}" alt="" width="150px"height="150px"></a>
                           </div>
                           <div class="folder-preview-title">
                               {{ folder.description }}
                           </div>
                       </div>
                   </div>
               {% endfor %}
           </div>
       </div>
   </div>



{% endblock %}
