{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Edit this User {{ user.username }}</h1>
                {{ form_start(form) }}
                {{ form_row(form.roles) }}
                <button type="submit" class="btn btn-primary" formnovalidate>Save</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}